`from django.urls import path, include

urlpatterns = [
    # Other user-related endpoints
    # path('profile/', views.profile_view, name='profile'),

    # Include API-related URLs
    # path('api/', include('users.api_urls')),
]